<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>WeatherApp</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
</head>
<body class="bg-gradient-to-br from-blue-200 to-indigo-300 min-h-screen text-gray-800">
  <div class="max-w-2xl mx-auto p-6">
    <header class="flex flex-col items-center gap-4">
      <h1 class="text-4xl font-bold">WeatherApp</h1>
      <div class="flex w-full max-w-md">
        <input id="cityInput" type="text" placeholder="Enter city" class="flex-1 p-2 rounded-l border border-gray-300"/>
        <button onclick="searchWeather()" class="bg-blue-600 text-white px-4 py-2 rounded-r">Search</button>
      </div>
    </header>

    <main class="mt-8 space-y-8">
      <section class="bg-white bg-opacity-60 p-4 rounded shadow">
        <h2 class="text-xl font-semibold mb-2">Current Weather</h2>
        <div class="flex items-center gap-4">
          <span class="material-symbols-outlined text-4xl" id="weatherIcon">cloud</span>
          <div>
            <div id="weather-desc">-</div>
            <div id="temp" class="text-2xl">--°C</div>
            <div id="feels-like">Feels like: --°C</div>
          </div>
        </div>
      </section>
    <div class="flex items-center gap-1 text-indigo-800 font-bold">
      <span class="material-symbols-outlined text-sm">location_on</span>
      <span id="current-location-name">Detecting location...</span>
    </div>
<div id="weather-desc">-</div>
      <section class="bg-white bg-opacity-60 p-4 rounded shadow">
        <h2 class="text-xl font-semibold mb-4">Hourly Forecast</h2>
        <div id="forecastCards" class="grid grid-cols-2 md:grid-cols-3 gap-4"></div>
      </section>

      <section class="mt-8 bg-white bg-opacity-60 p-4 rounded shadow">

      <div class="flex w-full max-w-md">
        <input id="cityInput" type="text" placeholder="Enter city" class="flex-1 p-2 rounded-l border border-gray-300"/>
        <button onclick="getAutoLocation()" class="bg-gray-100 border-y border-gray-300 px-3 flex items-center justify-center hover:bg-gray-200" title="Use my current location">
        <span class="material-symbols-outlined text-gray-600">location_on</span>
         </button>
         <button onclick="searchWeather()" class="bg-blue-600 text-white px-4 py-2 rounded-r">Search</button>
      </div>

      <h2 class="text-xl font-semibold mb-2">Chat with AI</h2>
      <div id="chatWindow" class="h-40 overflow-y-auto mb-4 p-2 border border-gray-200 rounded bg-white">
      </div>
      <div class="flex">
        <input id="chatInput" type="text" placeholder="Ask about the weather..." class="flex-1 p-2 rounded-l border border-gray-300"/>
        <button onclick="askAI()" class="bg-indigo-600 text-white px-4 py-2 rounded-r">Send</button>
      </div>
</section>
    </main>
    
    <footer class="text-center mt-12 text-gray-600">
      © 2025 WeatherApp
    </footer>
  </div>

  <script src="/scripts/scripts.js"></script>
</body>
</html>
